services:
  - type: web
    name: finpilot-api
    env: node
    plan: free
    region: oregon
    buildCommand: |
      corepack enable pnpm
      pnpm install --frozen-lockfile --prod=false
      pnpm --filter @painel/api prisma generate
      pnpm --filter @painel/api prisma migrate deploy
      pnpm --filter @painel/api prisma db seed
      pnpm --filter @painel/api build
    startCommand: pnpm --filter @painel/api start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
    healthCheckPath: /health

  - type: web
    name: finpilot-web
    env: static
    buildCommand: |
      corepack enable pnpm
      echo "VITE_API_URL=${VITE_API_URL}" > apps/web/.env.production
      pnpm install --frozen-lockfile
      pnpm --filter @painel/web build
    staticPublishPath: apps/web/dist
    envVars:
      - key: VITE_API_URL
        value: https://finpilot-api-ng5i.onrender.com
