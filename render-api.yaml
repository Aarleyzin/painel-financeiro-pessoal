services:
  - type: web
    name: finpilot-api
    env: node
    plan: free
    region: oregon
    buildCommand: |
      pnpm install --frozen-lockfile --prod=false
      pnpm --filter @painel/api prisma generate
      pnpm --filter @painel/api prisma migrate deploy
      pnpm --filter @painel/api prisma db seed
      pnpm --filter @painel/api build
    startCommand: pnpm --filter @painel/api start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
    healthCheckPath: /health
